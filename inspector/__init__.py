"""TAP Inspector (whitepaper §15) — a local developer tool to mint test
Passports, sign/verify Events, and visualize a record's chain, analogous to the
MCP Inspector. All logic lives in :mod:`inspector.core`; the CLI
(:mod:`inspector.cli`) and the local web UI (:mod:`inspector.app`) are both
thin front-ends over it.
"""
